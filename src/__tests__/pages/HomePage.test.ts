// TODO - add real unit tests for page

// import { render } from '@testing-library/react';

// @ts-ignore
const sum = (x: number, y: number) => x + y;

describe('Example test', () => {
  test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toBe(3);
  });
});
